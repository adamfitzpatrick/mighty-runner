import * as React from 'react'
import * as renderer from 'react-test-renderer'
import Effects from '.'

describe('Effects container', () => {
  test('should do nothing', () => {
    expect(true).toEqual(true)
  })
})
