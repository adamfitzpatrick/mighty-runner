import * as React from 'react'
import * as renderer from 'react-test-renderer'
import GearItem from '.'

describe('GearItem component', () => {
  test('should do nothing', () => {
    expect(true).toEqual(true)
  })
})
